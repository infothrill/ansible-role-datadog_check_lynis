init_config: null

# by default you will get all metrics if configured like this:
# instances: [{}]

# I prefer being explicit about submitting some that I am interested in:
instances:
  - metrics:
      - hardening_index
      - installed_packages
      - lynis_tests_done
    report: /var/log/lynis/report.dat
#    tags:
#      - fubar
